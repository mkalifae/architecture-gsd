---
phase: 05-self-design-validation-and-cli-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - workflows/resume.md
  - workflows/progress.md
autonomous: true

must_haves:
  truths:
    - "Running /arch-gsd:resume on a project with .arch/STATE.md displays current position, progress, decisions, and the exact next command from 'Resume with:' field"
    - "Running /arch-gsd:resume when no .arch/ exists displays a clear message directing user to /arch-gsd:new-system"
    - "Running /arch-gsd:progress displays project status with per-phase breakdown, artifact counts, and next recommended command"
    - "Running /arch-gsd:progress when no .arch/ exists displays a clear message directing user to /arch-gsd:new-system"
    - "Neither workflow reads full design documents into orchestrator context — only STATE.md (< 100 lines), CONTEXT.md head, ROADMAP.md head"
  artifacts:
    - path: "workflows/resume.md"
      provides: "Context-reset recovery workflow — reads STATE.md and presents continuation format"
      min_lines: 80
    - path: "workflows/progress.md"
      provides: "Read-only project status display from STATE.md and ROADMAP.md"
      min_lines: 60
  key_links:
    - from: "workflows/resume.md"
      to: ".arch/STATE.md"
      via: "Read tool — extracts Current Position, Session Continuity (Resume with:), Decisions, Blockers"
      pattern: "STATE\\.md"
    - from: "workflows/resume.md"
      to: ".arch/CONTEXT.md"
      via: "head -5 — domain field only for project name display"
      pattern: "CONTEXT\\.md"
    - from: "workflows/progress.md"
      to: ".arch/ROADMAP.md"
      via: "head -50 — phase list and status markers"
      pattern: "ROADMAP\\.md"
---

<objective>
Implement the /arch-gsd:resume and /arch-gsd:progress workflows — the two remaining stub workflows from Phase 1.

Purpose: /arch-gsd:resume enables context-reset survivability (STAT-05) — after a session ends, the user can run /arch-gsd:resume and immediately see where the project stands and what command to run next. /arch-gsd:progress provides a read-only status overview without prompting for action. Both are pure read-and-display workflows that read STATE.md (< 100 lines) and ROADMAP.md (head only) — no design document reads, no state changes (except resume updating Session Continuity in STATE.md).

Output: Two fully implemented workflow files replacing their Phase 1 stubs.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Reference implementations (adapt pattern, not copy):
@/home/mkali/.claude/get-shit-done/workflows/resume-project.md
@/home/mkali/.claude/get-shit-done/workflows/progress.md

Existing Architecture GSD workflows (established patterns to follow):
@workflows/execute-phase.md — STATE.md read pattern (head -30), context discipline
@workflows/verify-phase.md — STATE.md update pattern (Step 9), ROADMAP.md slug derivation
@workflows/new-system.md — discuss-system spawn pattern, STATE.md write template
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement /arch-gsd:resume workflow</name>
  <files>workflows/resume.md</files>
  <action>
Replace the stub content in workflows/resume.md with a complete workflow implementation. Preserve the existing frontmatter (description, allowed-tools) exactly as-is.

Follow the GSD resume-project.md pattern adapted for Architecture GSD's state shape. The workflow has 5 steps:

Step 1 — Validate project exists:
- Check: ls .arch/STATE.md .arch/CONTEXT.md 2>/dev/null
- If neither exists: display "No Architecture GSD project found. Run /arch-gsd:new-system first." and stop.

Step 2 — Read STATE.md:
- Read .arch/STATE.md in full (< 100 lines by enforcement)
- Extract: Current Position (Phase, Plan, Status), Session Continuity (Resume with:, Last session, Stopped at), Decisions (recent list), Blockers/Concerns, Progress bar
- Read .arch/CONTEXT.md domain field only: head -5 .arch/CONTEXT.md

Step 3 — Check for incomplete work:
- Check if ROADMAP.md exists: ls .arch/ROADMAP.md 2>/dev/null
- If ROADMAP.md missing: note "ROADMAP.md not yet generated" (degrade gracefully, do not fail)
- Check STATE.md status field for "partially complete" or "gaps_found" indicators

Step 4 — Present status:
- Display project name (from CONTEXT.md domain field)
- Display current phase, plan, status, last activity
- Display progress bar from STATE.md
- Display any concerns/blockers
- Display the "Resume with:" command as the primary continuation action

Step 5 — Determine next action and present continuation format:
- Parse the exact command from "Resume with:" field in STATE.md Session Continuity section
- Present it as the primary action
- Also offer: /arch-gsd:progress for status only
- If no "Resume with:" found, fall back to displaying available commands based on project state

Key differences from GSD resume-project.md (do NOT copy these GSD-specific features):
- No gsd-tools.js init resume command — read STATE.md directly with Read tool
- No .continue-here file concept — arch-gsd uses STATE.md "Resume with:" line
- No interrupted agent detection (no agent-history.json)
- No gsd-tools.js progress bar command — read Progress field from STATE.md directly
- No todos system — arch-gsd does not have .planning/todos/

Context discipline: resume.md reads ONLY .arch/STATE.md (full, < 100 lines) and .arch/CONTEXT.md (head -5 for domain field). For phase list, read ROADMAP.md head -30 only if it exists. Never read any design documents.

Use the same XML structure as existing Architecture GSD workflows (purpose, process with steps, success_criteria sections). Match the style of execute-phase.md and verify-phase.md.
  </action>
  <verify>
Verify the stub phrase is gone:
  node bin/arch-tools.js detect-stubs workflows/resume.md

Verify minimum length (expect 80+ lines):
  wc -l workflows/resume.md

Verify frontmatter is preserved:
  node bin/arch-tools.js frontmatter get workflows/resume.md

Verify the workflow references .arch/STATE.md (not .planning/STATE.md):
  grep -c "\.arch/STATE\.md" workflows/resume.md
  grep -c "\.planning/STATE\.md" workflows/resume.md  # should be 0
  </verify>
  <done>workflows/resume.md is a complete workflow (80+ lines) that reads .arch/STATE.md, presents project status, and displays the "Resume with:" command as the primary continuation action. No GSD-specific patterns (gsd-tools.js, .continue-here, agent-history.json) are present. Stub detection returns zero findings.</done>
</task>

<task type="auto">
  <name>Task 2: Implement /arch-gsd:progress workflow</name>
  <files>workflows/progress.md</files>
  <action>
Replace the stub content in workflows/progress.md with a complete workflow implementation. Preserve the existing frontmatter (description, allowed-tools) exactly as-is.

This is a read-only status display workflow — it makes NO state changes. No Write tool, no STATE.md updates, no git commits. It reads STATE.md and ROADMAP.md and displays formatted status.

The workflow has 4 steps:

Step 1 — Validate project exists:
- Check: ls .arch/STATE.md .arch/ROADMAP.md 2>/dev/null
- If STATE.md missing: display "No Architecture GSD project found. Run /arch-gsd:new-system first." and stop.
- If ROADMAP.md missing but STATE.md exists: display partial status from STATE.md only, note "ROADMAP.md not yet generated — run /arch-gsd:execute-phase 1 to start design pipeline."

Step 2 — Read state and roadmap:
- Read .arch/STATE.md in full (< 100 lines)
- Read .arch/ROADMAP.md head -50 (phase list section only)
- Read .arch/CONTEXT.md head -5 (domain field for project name)

Step 3 — Count design artifacts (optional enhancement):
- Bash: find .arch/phases/ -name "*.md" -o -name "*.yaml" 2>/dev/null | wc -l
- This gives total artifact count across all phases

Step 4 — Display formatted status report:
- Project name (from CONTEXT.md domain)
- Current phase and plan (from STATE.md Current Position)
- Overall progress percentage (from STATE.md Progress bar)
- Phase-by-phase status: for each phase in ROADMAP.md, show: phase number, name, status (complete/in-progress/planned)
- Total design artifacts count
- Recent decisions (last 3 from STATE.md Decisions section)
- Next recommended command (from STATE.md "Resume with:" field)

Format the output as a clean status report. Do NOT use emoji. Use plain text markers for status: [DONE] for complete phases, [ACTIVE] for current, [PLANNED] for future.

Context discipline: progress.md reads ONLY STATE.md (full), ROADMAP.md (head -50), CONTEXT.md (head -5). Never reads design documents, agent specs, or any other files.

The allowed-tools in frontmatter already correctly excludes Write, Edit, and Task — this is a read-only workflow. Verify the frontmatter does NOT include Write or Edit or Task.

Use the same XML structure as existing Architecture GSD workflows.
  </action>
  <verify>
Verify the stub phrase is gone:
  node bin/arch-tools.js detect-stubs workflows/progress.md

Verify minimum length (expect 60+ lines):
  wc -l workflows/progress.md

Verify frontmatter is preserved (should NOT have Write or Edit or Task):
  node bin/arch-tools.js frontmatter get workflows/progress.md

Verify the workflow references .arch/ paths:
  grep -c "\.arch/" workflows/progress.md
  </verify>
  <done>workflows/progress.md is a complete read-only workflow (60+ lines) that displays project status from STATE.md and ROADMAP.md. No state mutations (no Write/Edit/Task usage). Stub detection returns zero findings. Phase status markers use [DONE]/[ACTIVE]/[PLANNED] text (no emoji).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. Both workflows pass detect-stubs with zero findings
2. resume.md has 80+ lines, progress.md has 60+ lines
3. Neither workflow references .planning/ paths (only .arch/ paths)
4. resume.md includes "Resume with:" extraction logic
5. progress.md is read-only (no Write/Edit/Task tool references in body)
6. Both workflows use the same XML structure (purpose, process, success_criteria) as execute-phase.md and verify-phase.md
</verification>

<success_criteria>
- workflows/resume.md is a fully implemented workflow replacing its Phase 1 stub
- workflows/progress.md is a fully implemented workflow replacing its Phase 1 stub
- Both follow Architecture GSD's established workflow patterns (XML structure, context discipline, .arch/ state paths)
- Neither references GSD-specific tooling (gsd-tools.js, .planning/, .continue-here, agent-history.json)
- STAT-05 requirement is satisfied: /arch-gsd:resume restores context from STATE.md for session continuity
</success_criteria>

<output>
After completion, create `.planning/phases/05-self-design-validation-and-cli-polish/05-01-SUMMARY.md`
</output>
