---
phase: 05-self-design-validation-and-cli-polish
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - .arch/CONTEXT.md
  - .arch/STATE.md
  - .arch/RESEARCH.md
  - .arch/ROADMAP.md
  - .arch/phases/
autonomous: false

must_haves:
  truths:
    - "Architecture GSD successfully designs a real non-trivial agentic system (6 agents, mixed communication patterns) end-to-end"
    - "The output package includes all required document types: agent contracts, event schemas, topology, context flows, failure modes"
    - "The architecture package passes Level 1-4 verification with MANIFEST.md generated"
    - "The designed system has 5+ agents, at least 2 communication patterns (sync and event-driven), and non-trivial failure modes"
  artifacts:
    - path: ".arch/CONTEXT.md"
      provides: "Validated CONTEXT.md for Code Review Automation Pipeline"
      contains: "domain"
    - path: ".arch/ROADMAP.md"
      provides: "Design roadmap with phases for the external system"
      contains: "Phase"
    - path: ".arch/phases/"
      provides: "Design phases with architecture documents"
    - path: ".arch/phases/*/design/MANIFEST.md"
      provides: "Document index confirming package completeness"
      contains: "verification_status"
    - path: ".arch/phases/*/design/VERIFICATION.md"
      provides: "Level 1-4 verification results"
      contains: "status"
  key_links:
    - from: ".arch/CONTEXT.md"
      to: ".arch/ROADMAP.md"
      via: "arch-roadmapper derives phases from CONTEXT.md"
      pattern: "domain"
    - from: ".arch/phases/*/design/VERIFICATION.md"
      to: ".arch/phases/*/design/MANIFEST.md"
      via: "verify-phase generates MANIFEST.md after verification passes"
      pattern: "verification_status"
---

<objective>
Run Architecture GSD's full design pipeline against a real non-trivial agentic system — the Code Review Automation Pipeline (6 agents, mixed sync/event-driven communication) — to validate VALD-01: the pipeline produces a complete, verified architecture package end-to-end.

Purpose: This is the first true end-to-end test of the entire Architecture GSD pipeline. All 11 agents, all 3 core workflows (new-system, execute-phase, verify-phase), and all 4 verification levels are exercised against a real system description. A passing result proves the pipeline can produce the deliverable it promises.

Output: A complete architecture package in .arch/ for the Code Review Automation Pipeline, passing Level 1-4 verification with MANIFEST.md.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-self-design-validation-and-cli-polish/05-RESEARCH.md
@.planning/phases/05-self-design-validation-and-cli-polish/05-01-SUMMARY.md

Core workflows being exercised:
@workflows/new-system.md
@workflows/execute-phase.md
@workflows/verify-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full design pipeline on Code Review Automation Pipeline</name>
  <files>.arch/CONTEXT.md, .arch/STATE.md, .arch/RESEARCH.md, .arch/ROADMAP.md, .arch/phases/</files>
  <action>
This task runs the Architecture GSD pipeline end-to-end against a non-trivial external system. The executor orchestrates the existing workflows — it does NOT rewrite them.

IMPORTANT: If .arch/ already contains files from a previous run, back them up first:
  Bash: if [ -d .arch ] && [ -f .arch/CONTEXT.md ]; then mv .arch .arch-backup-$(date +%s); fi

**Step 1: Initialize the project via /arch-gsd:new-system**

Run the new-system workflow. Since this is an automated test run (not interactive intake), pre-create .arch/CONTEXT.md directly rather than spawning discuss-system interactively. This is the established "Skip" path from new-system.md Step 1.

Create .arch/ directory:
  Bash: mkdir -p .arch

Write .arch/CONTEXT.md describing the Code Review Automation Pipeline with all 6 required schema fields:

```yaml
---
domain: "Code Review Automation Pipeline"
actors:
  - "Software developers (submit PRs)"
  - "Tech leads (receive synthesized reviews)"
  - "DevOps team (maintains webhook infrastructure)"
non-goals:
  - "Code generation or auto-fix suggestions"
  - "IDE integration or real-time editing feedback"
  - "Repository management or branch operations"
  - "Human reviewer replacement — this augments, not replaces"
constraints:
  - "GitHub webhook event source (PR events only)"
  - "Must handle PRs up to 2000 changed lines"
  - "LLM-based logic review has latency budget of 60 seconds"
  - "All review comments must be traceable to a specific file and line range"
scale:
  agents: 6
  throughput: "50 PRs per hour peak"
  context_windows: "200K per agent"
locked-decisions:
  - "6 agents: trigger-listener, diff-analyzer, style-checker, logic-reviewer, synthesis-agent, notification-agent"
  - "Mixed communication: trigger-listener -> diff-analyzer is sync request/response; downstream agents use event-driven pub/sub"
  - "Style checking uses static analysis tools (not LLM) — deterministic and fast"
  - "Logic review uses LLM — non-deterministic, needs timeout and fallback"
  - "Synthesis agent resolves conflicting findings (style OK + logic fail) using severity ranking"
---
```

Validate the CONTEXT.md:
  Bash: node bin/arch-tools.js validate-context .arch/CONTEXT.md

If validation fails, fix the CONTEXT.md fields and re-validate until it passes.

Write .arch/STATE.md with initial state (follow the template from new-system.md Step 5):
- Phase: 0 (Intake complete)
- Status: CONTEXT.md produced and validated
- Resume with: /arch-gsd:execute-phase 1

**Step 2: Run the design pipeline for each phase**

Run /arch-gsd:execute-phase 1 by following the execute-phase.md workflow steps:
- This will spawn arch-researcher (produces .arch/RESEARCH.md)
- Then arch-roadmapper (produces .arch/ROADMAP.md with design phases)
- Then arch-planner + arch-checker loop (produces PLAN.md files)
- Then arch-executor per wave (produces design documents)

CRITICAL: Use Task() to spawn each agent per the execute-phase.md instructions. Follow execute-phase.md Steps 1-10 exactly. The executor IS the orchestrator for this pipeline run.

After Phase 1 completes, read .arch/ROADMAP.md to determine how many phases the roadmapper produced. Then run execute-phase for each subsequent phase in sequence.

For each phase N:
1. Follow execute-phase.md Steps 1-10
2. After Step 10, update .arch/STATE.md
3. Proceed to phase N+1

After all design phases complete, run verify-phase for the FINAL phase (the one containing the full architecture package):
1. Follow verify-phase.md Steps 1-9
2. Confirm VERIFICATION.md, INTEGRATION-REPORT.md, MANIFEST.md, and DIGEST.md are all produced

**Step 3: Validate the architecture package**

After the pipeline completes, verify the output meets VALD-01 criteria:

Check 1 — Agent contracts exist (expect 6):
  Bash: find .arch/phases/ -path "*/design/agents/*.md" | wc -l

Check 2 — Event schemas exist:
  Bash: find .arch/phases/ -path "*/design/events/*.yaml" | wc -l

Check 3 — Required document types present:
  Bash: find .arch/phases/ -path "*/design/topology/*" | wc -l
  Bash: find .arch/phases/ -path "*/design/context-flows/*" | wc -l
  Bash: find .arch/phases/ -path "*/design/failure-modes/*" | wc -l

Check 4 — MANIFEST.md exists and has verification_status:
  Bash: find .arch/phases/ -name "MANIFEST.md" -exec head -10 {} \;

Check 5 — VERIFICATION.md status:
  Bash: find .arch/phases/ -name "VERIFICATION.md" -exec node bin/arch-tools.js frontmatter get {} --field status \;

Record all check results for the checkpoint task.

If any critical document types are missing (no agent contracts, no event schemas, no MANIFEST.md), note the failures. The checkpoint task will present results to the human for judgment.
  </action>
  <verify>
Verify CONTEXT.md is valid:
  node bin/arch-tools.js validate-context .arch/CONTEXT.md

Verify STATE.md exists and shows pipeline progress:
  head -15 .arch/STATE.md

Verify ROADMAP.md was produced:
  head -30 .arch/ROADMAP.md

Verify design documents exist in at least one phase:
  find .arch/phases/ -path "*/design/*" -type f | wc -l

Verify MANIFEST.md was generated:
  find .arch/phases/ -name "MANIFEST.md" | head -1
  </verify>
  <done>The full Architecture GSD pipeline has run end-to-end on the Code Review Automation Pipeline. .arch/ contains CONTEXT.md (validated), STATE.md, RESEARCH.md, ROADMAP.md, and at least one design phase with agent contracts (6), event schemas, topology, context flows, failure modes, VERIFICATION.md, and MANIFEST.md. The pipeline completed without "failed" or "human_needed" stops.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verifies VALD-01 — complete architecture package</name>
  <files>.arch/phases/</files>
  <action>
Present the VALD-01 validation results to the human for review. The pipeline produced an architecture package for the Code Review Automation Pipeline (6-agent system with mixed sync/event-driven communication) including:
- Agent contracts for all 6 agents (trigger-listener, diff-analyzer, style-checker, logic-reviewer, synthesis-agent, notification-agent)
- Event schemas (PullRequestOpened, DiffAnalysisComplete, StyleCheckComplete, LogicReviewComplete, ReviewSynthesized, NotificationSent)
- Topology document, context flow maps, failure mode catalogs
- VERIFICATION.md with Level 1-4 results, MANIFEST.md with document index

Present the human with these verification commands to run:
1. find .arch/phases/ -path "*/design/*" -type f | sort
2. find .arch/phases/ -name "MANIFEST.md" -exec cat {} \;
3. find .arch/phases/ -name "VERIFICATION.md" -exec head -20 {} \;
4. find .arch/phases/ -name "*synthesis*" -exec cat {} \; (spot-check one agent)
5. Count agents (expect 6), events (expect 5+), failure modes documented

VALD-01 criteria: "The output package includes all required documents (agent contracts, event schemas, topology, context flows, failure modes, manifest) and passes Level 1-4 verification"

Wait for human verdict: "approved" to confirm VALD-01, or describe issues.
  </action>
  <verify>Human has reviewed the architecture package and provided a verdict.</verify>
  <done>Human confirmed VALD-01: the architecture package for the Code Review Automation Pipeline is complete, non-trivial (6 agents, mixed patterns), and passes Level 1-4 verification.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. .arch/CONTEXT.md passes validate-context
2. .arch/ contains a complete architecture package with all document types
3. MANIFEST.md exists with document index
4. VERIFICATION.md shows status "passed" or "gaps_found" (not "failed")
5. Human has reviewed and approved the architecture package (VALD-01)
6. The designed system has 6 agents and mixed communication patterns (non-trivial)
</verification>

<success_criteria>
- Architecture GSD designed a real non-trivial agentic system (6 agents, mixed patterns) end-to-end
- The output package includes agent contracts, event schemas, topology, context flows, failure modes, and MANIFEST.md
- Level 1-4 verification ran and produced results (passed or gaps_found — not failed)
- Human reviewer confirmed the package satisfies VALD-01
- STAT-05 can be tested: /arch-gsd:resume against .arch/STATE.md shows correct continuation state
</success_criteria>

<output>
After completion, create `.planning/phases/05-self-design-validation-and-cli-polish/05-02-SUMMARY.md`
</output>
