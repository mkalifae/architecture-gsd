---
phase: 05-self-design-validation-and-cli-polish
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - .arch-self/CONTEXT.md
  - .arch-self/STATE.md
  - .arch-self/RESEARCH.md
  - .arch-self/ROADMAP.md
  - .arch-self/phases/
  - .arch-self/DEFERRED.md
  - .arch-self/REVIEW-CHECKLIST.md
autonomous: false

must_haves:
  truths:
    - "Architecture GSD produces a complete architecture package for itself in .arch-self/, separate from .arch/"
    - "The self-design output includes specs for all 11 agents with names matching the actual agent specs"
    - "The self-design output includes event schemas matching the actual inter-agent state transitions"
    - "Self-design does not enter an infinite gap-closure loop — convergence criteria enforce max 2 correction rounds"
    - "DEFERRED.md is produced listing any gaps explicitly deferred after the iteration cap, rather than silently ignored"
    - "A human reviewer confirms the self-design output is sufficient for re-implementation from scratch using REVIEW-CHECKLIST.md"
  artifacts:
    - path: ".arch-self/CONTEXT.md"
      provides: "Architecture GSD described as the target system for self-design"
      contains: "domain"
    - path: ".arch-self/phases/"
      provides: "Self-design phases with architecture documents"
    - path: ".arch-self/REVIEW-CHECKLIST.md"
      provides: "Structured checklist for human reviewer to verify VALD-03"
      min_lines: 20
    - path: ".arch-self/DEFERRED.md"
      provides: "Explicit gap deferral register (may be empty if all checks pass)"
  key_links:
    - from: ".arch-self/CONTEXT.md"
      to: "agents/"
      via: "Self-design CONTEXT.md describes the actual 11-agent Architecture GSD system"
      pattern: "11.*agents|agent.*count"
    - from: ".arch-self/phases/*/design/agents/"
      to: "agents/"
      via: "Self-design agent specs should name all 11 actual agents"
      pattern: "arch-checker|arch-executor|arch-integrator|arch-planner|arch-researcher|arch-roadmapper|arch-verifier|context-engineer|discuss-system|failure-analyst|schema-designer"
    - from: ".arch-self/DEFERRED.md"
      to: ".arch-self/phases/*/design/VERIFICATION.md"
      via: "DEFERRED.md lists gaps from VERIFICATION.md that were not closed after convergence cap"
      pattern: "convergence_status.*deferred|GAP-"
---

<objective>
Run Architecture GSD's self-design: the system designs itself, producing an architecture package in .arch-self/ that describes the 11-agent Architecture GSD pipeline. Enforce convergence criteria (max 2 correction rounds), produce DEFERRED.md for any unresolved gaps, and create REVIEW-CHECKLIST.md for human verification. This validates VALD-02 (self-design produces complete package), VALD-03 (sufficient for re-implementation), and VALD-04 (convergence criteria prevent infinite loops).

Purpose: Self-design is the ultimate pipeline validation — if Architecture GSD can design itself and produce output that a developer could use to re-implement the system from scratch, then the pipeline is truly complete. The convergence criteria and DEFERRED.md protocol prevent the self-referential run from entering an infinite gap-closure loop.

Output: Complete self-design architecture package in .arch-self/, REVIEW-CHECKLIST.md, DEFERRED.md (if gaps remain), human sign-off on VALD-03.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-self-design-validation-and-cli-polish/05-RESEARCH.md
@.planning/phases/05-self-design-validation-and-cli-polish/05-01-SUMMARY.md
@.planning/phases/05-self-design-validation-and-cli-polish/05-02-SUMMARY.md

Self-design context — the actual system being described:
@workflows/execute-phase.md — main pipeline orchestrator
@workflows/verify-phase.md — verification pipeline
@workflows/new-system.md — intake workflow
@.planning/STATE.md — actual decisions list (locked-decisions source)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run self-design pipeline with convergence enforcement</name>
  <files>.arch-self/CONTEXT.md, .arch-self/STATE.md, .arch-self/RESEARCH.md, .arch-self/ROADMAP.md, .arch-self/phases/, .arch-self/DEFERRED.md, .arch-self/REVIEW-CHECKLIST.md</files>
  <action>
CRITICAL SAFETY: All self-design operations MUST use .arch-self/ directory. NEVER write to .arch/ or agents/ during this task. Pre-flight check:
  Bash: ls .arch-self/ 2>/dev/null || mkdir -p .arch-self/

**Step 1: Create self-design CONTEXT.md**

Write .arch-self/CONTEXT.md describing Architecture GSD as the target system. All 6 required schema fields must be populated with accurate values derived from the actual system:

```yaml
---
domain: "Architecture GSD — Multi-Agent Architecture Design System"
actors:
  - "Human architect (provides system description, reviews output, makes decisions)"
  - "Orchestrator workflows (new-system.md, execute-phase.md, verify-phase.md, resume.md, progress.md)"
  - "11 specialized agents (discuss-system, arch-researcher, arch-roadmapper, arch-planner, arch-checker, arch-executor, arch-verifier, arch-integrator, context-engineer, schema-designer, failure-analyst)"
  - "arch-tools.js CLI utility (frontmatter CRUD, stub detection, naming validation, context validation, 4-level verification engine, anti-pattern scanner, YAML graph traversal, digest writer)"
non-goals:
  - "Code generation from architecture documents — produces specs, not implementations"
  - "Runtime validation of designs — verification is structural, not behavioral"
  - "Infrastructure or deployment design"
  - "Coupling to specific protocols (MCP, A2A) — output is protocol-agnostic"
  - "Web or chat interface — CLI-first via Claude Code slash commands"
constraints:
  - "Claude Code CLI platform — slash commands, agent markdown specs, workflow files"
  - "All agents executed as Claude API calls via Task() with model profiles (Opus, Sonnet, Haiku)"
  - "Zero external Node.js dependencies for arch-tools.js core (js-yaml acceptable for YAML parsing)"
  - "Disk-as-shared-memory — all agent communication via files, no in-memory state"
  - "STATE.md must survive context resets — any agent resumes from disk state alone"
  - "200K context window per agent — orchestrators stay at ~15% utilization"
scale:
  agents: 11
  throughput: "One design run per session"
  context_windows: "200K per agent, orchestrators at ~15%"
locked-decisions:
  - "All 11 agents in v1 — complete architecture packages require all specializations"
  - "Plugin structure from day one — standalone first, plugin.json migration additive only"
  - "Zero external Node.js deps for arch-tools.js core"
  - "Dual-format output (markdown + YAML) mandatory for programmatic verification"
  - "Adversarial prompting for arch-checker (plans) vs arch-verifier (outputs) — separate failure domains"
  - "4-level verification: Exists -> Substantive -> Cross-Referenced -> Internally Consistent"
  - "Wave-based parallel execution — plans grouped by wave, same-wave plans spawned simultaneously"
  - "Bounded revision loops — max 3 planner-checker iterations before human escalation"
  - "STATE.md after every plan completion (max 100 lines) — mandatory pre-read for all agents"
  - "Phase-boundary DIGEST.md (max 50 lines) — prevents context overflow for cross-phase checks"
---
```

Validate: node bin/arch-tools.js validate-context .arch-self/CONTEXT.md

**Step 2: Initialize self-design STATE.md**

Write .arch-self/STATE.md using the same template as new-system.md Step 5:
- Core value: "Given a description of an agentic system, produce a complete, internally consistent, cross-referenced architecture package"
- Phase: 0 (Intake complete)
- Resume with: /arch-gsd:execute-phase 1

**Step 3: Run the design pipeline with convergence enforcement**

Run the pipeline using the same pattern as plan 05-02 Task 1, but ALL operations target .arch-self/ paths:

For each subagent spawned via Task(), explicitly specify:
- CONTEXT path: .arch-self/CONTEXT.md (NOT .arch/CONTEXT.md)
- STATE file: .arch-self/STATE.md
- RESEARCH path: .arch-self/RESEARCH.md
- ROADMAP path: .arch-self/ROADMAP.md
- Phase directory: .arch-self/phases/{phase-slug}/

Follow execute-phase.md Steps 1-10 for each design phase, with all paths redirected to .arch-self/.

After all design phases execute, run verify-phase for the final phase with convergence enforcement:

```
correction_round = 0
max_corrections = 2

Run verify-phase Steps 1-9 for the final phase (all paths under .arch-self/)
Parse VERIFICATION.md status.

While status == "gaps_found" AND correction_round < max_corrections:
  correction_round += 1
  Record gap_count_before = count of findings in VERIFICATION.md

  Run execute-phase in correction mode for the affected phase:
    - Re-run arch-executor only on plans/documents that have gaps
    - Do NOT re-run arch-planner or arch-checker

  Run verify-phase again
  Parse VERIFICATION.md status
  Record gap_count_after = count of findings

  If gap_count_after >= gap_count_before:
    # System is looping, not converging — stop immediately
    Break

If gaps remain after max_corrections OR loop detected:
  Write .arch-self/DEFERRED.md following the DEFERRED.md protocol from research:
    - Frontmatter: phase, deferred_at (ISO timestamp), iteration_count, convergence_status: deferred
    - For each remaining gap: GAP-NN with level, file, detail, why deferred, remediation path
    - "What This Means" section explaining partial verification
    - "Next Steps" section with specific recommendations

If all checks pass (status == "passed"):
  Write .arch-self/DEFERRED.md with minimal content:
    - convergence_status: passed
    - "No gaps deferred — all Level 1-4 checks passed"
```

**Step 4: Validate self-design output against actual system**

Check 1 — Agent count (expect 11 agent specs in self-design output):
  Bash: find .arch-self/phases/ -path "*/design/agents/*.md" 2>/dev/null | wc -l

Check 2 — Agent names match actual agents:
  Bash: find .arch-self/phases/ -path "*/design/agents/*.md" 2>/dev/null -exec basename {} .md \; | sort
  Bash: ls agents/ | sed 's/\.md$//' | sort
  Compare the two lists. Note any missing or extra agents.

Check 3 — Event schemas exist with inter-agent state transitions:
  Bash: find .arch-self/phases/ -path "*/design/events/*.yaml" 2>/dev/null | head -5

Check 4 — MANIFEST.md exists:
  Bash: find .arch-self/phases/ -name "MANIFEST.md" 2>/dev/null | head -1

**Step 5: Create REVIEW-CHECKLIST.md for human reviewer (VALD-03)**

Write .arch-self/REVIEW-CHECKLIST.md — a structured checklist that the human reviewer will use to verify the self-design output is "sufficient for re-implementation from scratch." The checklist must include:

```markdown
# Self-Design Review Checklist

**Reviewed by:** [human fills in]
**Date:** [human fills in]
**Overall verdict:** [PASS / FAIL / PARTIAL]

## Completeness Checks

- [ ] All 11 agents have specs in self-design output
  - [ ] arch-checker
  - [ ] arch-executor
  - [ ] arch-integrator
  - [ ] arch-planner
  - [ ] arch-researcher
  - [ ] arch-roadmapper
  - [ ] arch-verifier
  - [ ] context-engineer
  - [ ] discuss-system
  - [ ] failure-analyst
  - [ ] schema-designer

- [ ] Event schemas exist with typed payloads (no any/object types)
- [ ] Topology document describes agent communication patterns
- [ ] Context flow maps show data movement between agents
- [ ] Failure mode catalogs exist with concrete recovery actions

## Structural Checks

- [ ] MANIFEST.md exists with document index and reading order
- [ ] VERIFICATION.md exists with Level 1-4 results
- [ ] No agent spec has TBD or placeholder sections
- [ ] No event field uses "any" or "object" as its type

## Sufficiency for Re-Implementation

- [ ] A developer reading ONLY the self-design package (not the actual code) could understand what each agent does
- [ ] The communication patterns between agents are clear from topology + event schemas
- [ ] Failure modes describe what happens when each agent fails, not just that it "handles errors gracefully"
- [ ] The orchestration model (who spawns whom, in what order) is documented

## Convergence

- [ ] DEFERRED.md exists (even if convergence_status is "passed")
- [ ] If gaps were deferred, remediation paths are documented
- [ ] The iteration count is recorded (max 2 correction rounds enforced)

## Notes

[Human adds observations here]
```

Record all validation results from Step 4 for the checkpoint task.
  </action>
  <verify>
Verify self-design used .arch-self/ (not .arch/):
  ls .arch-self/CONTEXT.md .arch-self/STATE.md

Verify CONTEXT.md is valid:
  node bin/arch-tools.js validate-context .arch-self/CONTEXT.md

Verify agent specs were produced:
  find .arch-self/phases/ -path "*/design/agents/*.md" 2>/dev/null | wc -l

Verify DEFERRED.md exists:
  ls .arch-self/DEFERRED.md

Verify REVIEW-CHECKLIST.md exists:
  ls .arch-self/REVIEW-CHECKLIST.md

Verify .arch/ was NOT modified (should match pre-self-design state):
  git diff --name-only -- .arch/
  </verify>
  <done>Self-design pipeline ran in .arch-self/ directory (not .arch/). CONTEXT.md describes Architecture GSD with all 11 agents. Convergence enforcement limited corrections to max 2 rounds. DEFERRED.md records convergence status (passed or deferred with explicit gap list). REVIEW-CHECKLIST.md is ready for human review. The self-design output includes agent specs, event schemas, and supporting documents.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human reviews self-design for re-implementation sufficiency (VALD-02, VALD-03, VALD-04)</name>
  <files>.arch-self/REVIEW-CHECKLIST.md</files>
  <action>
Present the self-design validation results to the human for review using REVIEW-CHECKLIST.md. Architecture GSD designed itself in .arch-self/ (separate from .arch/) with convergence enforcement (max 2 correction rounds). The output includes:
- Agent specs for all 11 Architecture GSD agents
- Event schemas for inter-agent state transitions
- Topology, context flows, and failure mode documents
- VERIFICATION.md with Level 1-4 results, MANIFEST.md with document index
- DEFERRED.md tracking convergence status and any deferred gaps
- REVIEW-CHECKLIST.md for structured human review

Guide the human through the checklist:

1. Open the checklist: cat .arch-self/REVIEW-CHECKLIST.md

2. Verify agent count matches actual system:
   find .arch-self/phases/ -path "*/design/agents/*.md" | wc -l   (expect 11)
   ls agents/ | wc -l                                              (compare)

3. Verify agent names match:
   find .arch-self/phases/ -path "*/design/agents/*.md" -exec basename {} .md \; | sort
   ls agents/ | sed 's/\.md$//' | sort

4. Read DEFERRED.md for convergence status: cat .arch-self/DEFERRED.md

5. Read MANIFEST.md for document index:
   find .arch-self/phases/ -name "MANIFEST.md" -exec cat {} \;

6. Spot-check 2-3 agent specs for completeness (should have Role, Inputs, Outputs, State, Failure Modes — no TBD):
   find .arch-self/phases/ -path "*/design/agents/arch-planner*" -exec cat {} \;
   find .arch-self/phases/ -path "*/design/agents/arch-verifier*" -exec cat {} \;

7. The key question for VALD-03: "Could a new developer use this package alone (no code access) to reconstruct an equivalent system?"

Wait for human to fill in REVIEW-CHECKLIST.md and provide verdict: "approved" with PASS/FAIL/PARTIAL, or describe specific issues.
  </action>
  <verify>Human has reviewed REVIEW-CHECKLIST.md and provided a verdict (PASS, FAIL, or PARTIAL).</verify>
  <done>Human confirmed VALD-03: self-design output is sufficient for re-implementation from scratch. REVIEW-CHECKLIST.md has been filled in with the human's verdict and observations.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. .arch-self/ contains a complete self-design architecture package (separate from .arch/)
2. Agent count in self-design output is 11, matching the actual agent count
3. DEFERRED.md exists with convergence_status field (passed or deferred)
4. If convergence_status is "deferred", iteration_count shows max 2 correction rounds
5. REVIEW-CHECKLIST.md exists and has been reviewed by human
6. Human confirmed VALD-03: self-design output is sufficient for re-implementation from scratch
7. .arch/ directory was NOT modified by the self-design run
</verification>

<success_criteria>
- VALD-02: Architecture GSD produced a complete architecture package for itself in .arch-self/
- VALD-03: Human reviewer confirmed the self-design output is sufficient for re-implementation (signed off on REVIEW-CHECKLIST.md)
- VALD-04: Self-design had explicit convergence criteria — max 2 correction rounds enforced, DEFERRED.md produced for any remaining gaps
- Self-design did NOT pollute .arch/ — all output is in .arch-self/
- All 11 agent names appear in the self-design output
</success_criteria>

<output>
After completion, create `.planning/phases/05-self-design-validation-and-cli-polish/05-03-SUMMARY.md`
</output>
