---
phase: 02-internet-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/arch-researcher.md
  - agents/arch-planner.md
autonomous: true

must_haves:
  truths:
    - "arch-researcher agent spec lists WebSearch in its tools frontmatter"
    - "arch-researcher agent spec lists Context7 in its tools frontmatter"
    - "arch-planner agent spec lists WebFetch in its tools frontmatter"
    - "arch-planner agent spec lists Context7 in its tools frontmatter"
  artifacts:
    - path: "agents/arch-researcher.md"
      provides: "Internet-aware researcher with WebSearch and Context7"
      contains: "tools: Read, Write, Bash, Grep, Glob, WebFetch, WebSearch, Context7"
    - path: "agents/arch-planner.md"
      provides: "Internet-aware planner with WebFetch and Context7"
      contains: "tools: Read, Write, Edit, Bash, Grep, Glob, WebFetch, Context7"
  key_links:
    - from: "agents/arch-researcher.md"
      to: "execution_flow Step 4"
      via: "WebSearch used for domain pattern queries; Context7 used for library doc lookups"
      pattern: "WebSearch|Context7"
    - from: "agents/arch-planner.md"
      to: "execution_flow Step 4"
      via: "WebFetch used for documentation fetching; Context7 used for library-specific docs"
      pattern: "WebFetch|Context7"
---

<objective>
Add internet access tools (WebSearch, Context7, WebFetch) to arch-researcher and arch-planner agent specs, closing INET-01 through INET-04.

Purpose: These agents need live documentation and pattern search capabilities. arch-researcher already references Context7 in its execution_flow body but lacks it in the tools frontmatter. arch-planner has no internet tools at all. Adding these tools aligns both agents with GSD's proven internet-aware research and planning patterns.

Output: Two updated agent spec files with correct tools frontmatter.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-permission-boundaries/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WebSearch and Context7 to arch-researcher tools (INET-01, INET-02)</name>
  <files>agents/arch-researcher.md</files>
  <action>
Edit the YAML frontmatter tools line in agents/arch-researcher.md.

Current value:
```
tools: Read, Write, Bash, Grep, Glob, WebFetch
```

Change to:
```
tools: Read, Write, Bash, Grep, Glob, WebFetch, WebSearch, Context7
```

This adds WebSearch (INET-01: web search for architecture patterns and case studies) and Context7 (INET-02: framework and library documentation queries). WebFetch is already present from the original spec.

Follow the tool ordering convention established in Phase 1: core tools first (Read, Write, Bash, Grep, Glob), then domain-specific internet tools (WebFetch, WebSearch, Context7).

The body already references Context7 in Steps 4 and 5 of execution_flow — the frontmatter was inconsistent with the body. Do NOT modify the body; only change the tools frontmatter line.
  </action>
  <verify>
Run: grep "^tools:" agents/arch-researcher.md
Expected output: tools: Read, Write, Bash, Grep, Glob, WebFetch, WebSearch, Context7

Verify WebSearch is present: grep -c "WebSearch" agents/arch-researcher.md should return at least 1
Verify Context7 is in frontmatter: head -6 agents/arch-researcher.md | grep "Context7"
  </verify>
  <done>arch-researcher tools frontmatter contains WebSearch (INET-01) and Context7 (INET-02) alongside existing WebFetch. Body unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add WebFetch and Context7 to arch-planner tools (INET-03, INET-04)</name>
  <files>agents/arch-planner.md</files>
  <action>
Edit the YAML frontmatter tools line in agents/arch-planner.md.

Current value:
```
tools: Read, Write, Edit, Bash, Grep, Glob
```

Change to:
```
tools: Read, Write, Edit, Bash, Grep, Glob, WebFetch, Context7
```

This adds WebFetch (INET-03: fetch documentation when decomposing phases into tasks) and Context7 (INET-04: query library-specific documentation during planning).

Follow the tool ordering convention: core tools first (Read, Write, Edit, Bash, Grep, Glob), then domain-specific internet tools (WebFetch, Context7). Note arch-planner retains Edit (per key decision: planner iterates with checker in a feedback loop).

Do NOT modify the body. The tools frontmatter addition is sufficient — arch-planner's execution_flow will naturally use WebFetch and Context7 when available, just as arch-researcher's body already references Context7 for library lookups.
  </action>
  <verify>
Run: grep "^tools:" agents/arch-planner.md
Expected output: tools: Read, Write, Edit, Bash, Grep, Glob, WebFetch, Context7

Verify WebFetch is present: grep -c "WebFetch" agents/arch-planner.md should return at least 1
Verify Context7 is in frontmatter: head -6 agents/arch-planner.md | grep "Context7"
  </verify>
  <done>arch-planner tools frontmatter contains WebFetch (INET-03) and Context7 (INET-04) alongside existing core tools. Edit retained per key decision. Body unchanged.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. Confirm all 4 INET requirements are covered:
   - INET-01: grep "WebSearch" agents/arch-researcher.md (tools line)
   - INET-02: grep "Context7" agents/arch-researcher.md (tools line)
   - INET-03: grep "WebFetch" agents/arch-planner.md (tools line)
   - INET-04: grep "Context7" agents/arch-planner.md (tools line)

2. Confirm no unintended changes:
   - wc -l agents/arch-researcher.md should match pre-edit line count (319 lines from Phase 1 summary)
   - wc -l agents/arch-planner.md should match pre-edit line count
   - Only line 4 (tools frontmatter) should differ in each file

3. Confirm body integrity:
   - arch-researcher body still references Context7 in Steps 4 and 5
   - arch-planner body unchanged (still zero WebFetch/Context7 body references — that is correct)
</verification>

<success_criteria>
All 4 success criteria from ROADMAP.md Phase 2 are satisfied:
1. arch-researcher agent spec lists WebSearch in its tools section
2. arch-researcher agent spec lists Context7 in its tools section
3. arch-planner agent spec lists WebFetch in its tools section
4. arch-planner agent spec lists Context7 in its tools section
</success_criteria>

<output>
After completion, create `.planning/phases/02-internet-access/02-01-SUMMARY.md`
</output>
