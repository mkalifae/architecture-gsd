---
phase: 01-foundation-tooling-and-agent-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - plugin.json
  - config.json
  - agents/discuss-system.md
  - agents/arch-researcher.md
  - agents/arch-roadmapper.md
  - agents/arch-planner.md
  - agents/arch-checker.md
  - agents/arch-executor.md
  - agents/arch-verifier.md
  - agents/arch-integrator.md
  - agents/context-engineer.md
  - agents/schema-designer.md
  - agents/failure-analyst.md
  - workflows/new-system.md
  - workflows/execute-phase.md
  - workflows/plan-phase.md
  - workflows/verify-phase.md
  - workflows/progress.md
  - workflows/resume.md
  - bin/.gitkeep
  - templates/.gitkeep
  - references/.gitkeep
autonomous: true

must_haves:
  truths:
    - "plugin.json exists with name 'arch-gsd' and correct commands/agents paths"
    - "config.json has model_profiles with balanced profile matching FOUN-08 exactly"
    - "All 11 agent stubs exist in agents/ with correct name, model, and tools frontmatter"
    - "All 6 workflow stubs exist in workflows/ with description and allowed-tools frontmatter"
    - "bin/, templates/, references/ directories exist"
  artifacts:
    - path: "plugin.json"
      provides: "Claude Code plugin manifest"
      contains: "arch-gsd"
    - path: "config.json"
      provides: "Model profiles and workflow configuration"
      contains: "model_profiles"
    - path: "agents/discuss-system.md"
      provides: "Agent stub with Opus model assignment"
      contains: "model: opus"
    - path: "agents/arch-checker.md"
      provides: "Agent stub with Haiku model assignment"
      contains: "model: haiku"
    - path: "workflows/new-system.md"
      provides: "Workflow stub with slash command frontmatter"
      contains: "allowed-tools"
  key_links:
    - from: "plugin.json"
      to: "agents/"
      via: "agents field pointing to directory"
      pattern: "\"agents\":\\s*\"agents/\""
    - from: "plugin.json"
      to: "workflows/"
      via: "commands field pointing to directory"
      pattern: "\"commands\":\\s*\"workflows/\""
    - from: "config.json"
      to: "agents/*.md"
      via: "model_profiles keys matching agent stub names"
      pattern: "discuss-system.*opus"
---

<objective>
Create the complete plugin directory structure, plugin.json manifest, config.json with model profiles, 11 agent stubs with final-correct frontmatter, and 6 workflow stubs with slash command frontmatter.

Purpose: Establish the project skeleton that every subsequent plan and phase depends on. Agent stubs must have correct model and tools values from day one (per FOUN-08) so Phase 2+ agents inherit correct routing without rework.

Output: A fully scaffolded plugin directory that Claude Code can load, with all directories, manifest, config, and stub files in place.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-tooling-and-agent-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create plugin manifest, config, and directory skeleton</name>
  <files>
    plugin.json
    config.json
    bin/.gitkeep
    templates/.gitkeep
    references/.gitkeep
  </files>
  <action>
Create the plugin root directory structure and two JSON config files.

**plugin.json** — Claude Code plugin manifest:
```json
{
  "name": "arch-gsd",
  "version": "0.1.0",
  "description": "Multi-agent architecture design system. Produces verified architecture packages for agentic systems.",
  "commands": "workflows/",
  "agents": "agents/"
}
```

**config.json** — Model profiles and workflow flags. Must match FOUN-08 exactly:
- Opus: discuss-system, arch-roadmapper, context-engineer
- Sonnet: arch-researcher, arch-planner, arch-executor, arch-verifier, schema-designer, failure-analyst
- Haiku: arch-checker, arch-integrator

Include three profile tiers (quality, balanced, budget) following GSD's pattern. The "balanced" profile IS the FOUN-08 allocation. Include workflow flags: research, plan_check, verifier, parallelization, commit_docs.

Create empty directories `bin/`, `templates/`, `references/` with .gitkeep files so git tracks them.
  </action>
  <verify>
Run: `cat plugin.json | node -e "const j=JSON.parse(require('fs').readFileSync('/dev/stdin','utf-8')); console.log(j.name === 'arch-gsd' && j.commands === 'workflows/' && j.agents === 'agents/' ? 'PASS' : 'FAIL')"`
Run: `cat config.json | node -e "const j=JSON.parse(require('fs').readFileSync('/dev/stdin','utf-8')); const b=j.model_profiles.balanced; console.log(b['discuss-system']==='opus' && b['arch-checker']==='haiku' && b['arch-executor']==='sonnet' ? 'PASS' : 'FAIL')"`
Run: `ls bin/ templates/ references/`
  </verify>
  <done>plugin.json has name "arch-gsd" with commands and agents paths. config.json has three model profiles where balanced matches FOUN-08 exactly. bin/, templates/, references/ directories exist.</done>
</task>

<task type="auto">
  <name>Task 2: Create 11 agent stubs and 6 workflow stubs with final-correct frontmatter</name>
  <files>
    agents/discuss-system.md
    agents/arch-researcher.md
    agents/arch-roadmapper.md
    agents/arch-planner.md
    agents/arch-checker.md
    agents/arch-executor.md
    agents/arch-verifier.md
    agents/arch-integrator.md
    agents/context-engineer.md
    agents/schema-designer.md
    agents/failure-analyst.md
    workflows/new-system.md
    workflows/execute-phase.md
    workflows/plan-phase.md
    workflows/verify-phase.md
    workflows/progress.md
    workflows/resume.md
  </files>
  <action>
Create all 11 agent stubs in `agents/`. Each stub must have COMPLETE YAML frontmatter with final-correct values. The body is a one-liner placeholder with section stubs.

**Agent frontmatter fields (all required):**
- `name`: kebab-case agent name (e.g., `discuss-system`)
- `description`: one sentence describing what this agent does and when it's spawned
- `tools`: comma-separated list of Claude Code tools this agent needs
- `model`: one of opus, sonnet, haiku — per FOUN-08 balanced profile
- `color`: a distinct color per agent for terminal output

**Agent model assignments (FOUN-08 — non-negotiable):**
| Agent | Model | Tools | Color |
|-------|-------|-------|-------|
| discuss-system | opus | Read, Write, Edit, Bash, Grep, Glob | blue |
| arch-researcher | sonnet | Read, Bash, Grep, Glob, WebFetch | cyan |
| arch-roadmapper | opus | Read, Write, Edit, Bash, Grep, Glob | green |
| arch-planner | sonnet | Read, Write, Edit, Bash, Grep, Glob | yellow |
| arch-checker | haiku | Read, Bash, Grep, Glob | red |
| arch-executor | sonnet | Read, Write, Edit, Bash, Grep, Glob | magenta |
| arch-verifier | sonnet | Read, Bash, Grep, Glob | orange |
| arch-integrator | haiku | Read, Bash, Grep, Glob | purple |
| context-engineer | opus | Read, Write, Edit, Bash, Grep, Glob | teal |
| schema-designer | sonnet | Read, Write, Edit, Bash, Grep, Glob | pink |
| failure-analyst | sonnet | Read, Write, Edit, Bash, Grep, Glob | brown |

**Agent stub body format** (same for all, with section placeholders):
```markdown
<!-- Phase 1 stub — implementation in Phase N -->
<!-- Sections below will be populated when this agent is implemented -->

<role>
<!-- To be implemented -->
</role>

<upstream_input>
<!-- To be implemented -->
</upstream_input>

<downstream_consumer>
<!-- To be implemented -->
</downstream_consumer>

<execution_flow>
<!-- To be implemented -->
</execution_flow>

<structured_returns>
<!-- To be implemented -->
</structured_returns>

<failure_modes>
<!-- To be implemented -->
</failure_modes>

<constraints>
<!-- To be implemented -->
</constraints>
```

**6 Workflow stubs in `workflows/`:**

Each workflow stub must have correct slash command frontmatter:

| Workflow | Description | Allowed Tools | Argument Hint |
|----------|-------------|---------------|---------------|
| new-system.md | Start Architecture GSD for a new system | Read, Write, Edit, Bash, Grep, Glob, Task | [system-description] |
| execute-phase.md | Execute a design phase | Read, Write, Edit, Bash, Grep, Glob, Task | [phase-number] |
| plan-phase.md | Plan a design phase | Read, Write, Edit, Bash, Grep, Glob, Task | [phase-number] |
| verify-phase.md | Verify a completed design phase | Read, Write, Edit, Bash, Grep, Glob, Task | [phase-number] |
| progress.md | Show Architecture GSD progress | Read, Bash, Grep, Glob | (none) |
| resume.md | Resume Architecture GSD from saved state | Read, Write, Edit, Bash, Grep, Glob, Task | (none) |

Workflow stub body: `# Stub -- Implementation in Phase N`

Do NOT use `model: inherit` in any agent spec. Do NOT omit the tools field. Do NOT use generic descriptions like "An agent" — each description must state what the agent does specifically.
  </action>
  <verify>
Run: `ls agents/*.md | wc -l` — expect 11
Run: `ls workflows/*.md | wc -l` — expect 6
Run: `node -e "const fs=require('fs'); const agents=['discuss-system','arch-researcher','arch-roadmapper','arch-planner','arch-checker','arch-executor','arch-verifier','arch-integrator','context-engineer','schema-designer','failure-analyst']; const models={discuss_system:'opus',arch_researcher:'sonnet',arch_roadmapper:'opus',arch_planner:'sonnet',arch_checker:'haiku',arch_executor:'sonnet',arch_verifier:'sonnet',arch_integrator:'haiku',context_engineer:'opus',schema_designer:'sonnet',failure_analyst:'sonnet'}; let pass=true; for(const a of agents){const c=fs.readFileSync('agents/'+a+'.md','utf-8'); const m=c.match(/model:\\s*(\\w+)/); if(!m||m[1]!==models[a.replace(/-/g,'_')]){console.log('FAIL: '+a+' model'); pass=false;}} console.log(pass?'ALL PASS':'SOME FAILED');"`
Run: `grep -l "allowed-tools" workflows/*.md | wc -l` — expect 6
  </verify>
  <done>All 11 agent stubs exist with correct model assignments matching FOUN-08. All 6 workflow stubs exist with valid slash command frontmatter. No agent uses "model: inherit" or has a missing tools field.</done>
</task>

</tasks>

<verification>
- `plugin.json` is valid JSON with correct name and paths
- `config.json` has three model profiles with balanced matching FOUN-08
- All 11 agents in `agents/` have name, description, tools, model, color in frontmatter
- All 6 workflows in `workflows/` have description and allowed-tools in frontmatter
- `bin/`, `templates/`, `references/` directories exist
- No agent stub has `model: inherit` or missing `tools:` field
</verification>

<success_criteria>
- FOUN-01: Plugin directory structure complete (agents/, workflows/, bin/, templates/, references/, plugin.json)
- FOUN-08: Model profiles configured in config.json with correct per-agent assignments
- All 17 stub files (11 agents + 6 workflows) have final-correct frontmatter
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-tooling-and-agent-scaffold/01-01-SUMMARY.md`
</output>
