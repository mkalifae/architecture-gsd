---
phase: 01-permission-boundaries
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/arch-roadmapper.md
  - agents/arch-verifier.md
  - agents/arch-researcher.md
autonomous: true

must_haves:
  truths:
    - "arch-roadmapper has Write but NOT Edit in its tools list"
    - "arch-verifier has Write in its tools list (previously had none)"
    - "arch-researcher has Write in its tools list (previously had none)"
    - "No other agent spec files are modified"
    - "Agent spec body content (role, execution_flow, etc.) is unchanged"
  artifacts:
    - path: "agents/arch-roadmapper.md"
      provides: "Roadmapper agent spec with write-once permission"
      contains: "tools: Read, Write, Bash, Grep, Glob"
    - path: "agents/arch-verifier.md"
      provides: "Verifier agent spec with Write for VERIFICATION.md output"
      contains: "tools: Read, Write, Bash, Grep, Glob"
    - path: "agents/arch-researcher.md"
      provides: "Researcher agent spec with Write for RESEARCH.md output"
      contains: "tools: Read, Write, Bash, Grep, Glob, WebFetch"
  key_links:
    - from: "agents/arch-roadmapper.md"
      to: "ROADMAP.md output"
      via: "Write tool in execution_flow Step 8"
      pattern: "tools:.*Write"
    - from: "agents/arch-verifier.md"
      to: "VERIFICATION.md output"
      via: "Write tool in execution_flow Step 8"
      pattern: "tools:.*Write"
    - from: "agents/arch-researcher.md"
      to: "RESEARCH.md output"
      via: "Write tool in execution_flow Step 7"
      pattern: "tools:.*Write"
---

<objective>
Update the tools frontmatter line in three agent spec files to enforce principled permission boundaries: roadmapper loses Edit (write-once pattern), verifier and researcher gain Write (produce own output files).

Purpose: Each agent has exactly the file-mutation tools its role requires. Roadmapper creates ROADMAP.md once (Write, not Edit). Verifier produces VERIFICATION.md (needs Write). Researcher produces RESEARCH.md (needs Write). This closes requirements PERM-02, PERM-04, and PERM-05.

Output: Three modified agent spec files with corrected tools frontmatter.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@agents/arch-roadmapper.md
@agents/arch-verifier.md
@agents/arch-researcher.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Edit from arch-roadmapper tools (PERM-02)</name>
  <files>agents/arch-roadmapper.md</files>
  <action>
Edit the YAML frontmatter `tools:` line in agents/arch-roadmapper.md.

Current line 4: `tools: Read, Write, Edit, Bash, Grep, Glob`
Change to: `tools: Read, Write, Bash, Grep, Glob`

This removes Edit while keeping Write. The roadmapper creates ROADMAP.md (write-once) and does not modify existing files. Its execution_flow Step 8 already says "Write .arch/ROADMAP.md using the Write tool" — Edit was never referenced in the body, so only the frontmatter line changes.

Do NOT modify any other content in the file. The body (role, upstream_input, downstream_consumer, execution_flow, structured_returns, failure_modes, constraints) must remain identical.
  </action>
  <verify>
Run: `head -6 agents/arch-roadmapper.md` — line 4 should read `tools: Read, Write, Bash, Grep, Glob` (no Edit).
Run: `grep -c 'Edit' agents/arch-roadmapper.md` — should return `0` (Edit removed from the only place it appeared).
Run: `wc -l agents/arch-roadmapper.md` — line count should be 347 (unchanged from original).
  </verify>
  <done>agents/arch-roadmapper.md frontmatter has `tools: Read, Write, Bash, Grep, Glob` with no Edit. File body unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Add Write to arch-verifier tools (PERM-04)</name>
  <files>agents/arch-verifier.md</files>
  <action>
Edit the YAML frontmatter `tools:` line in agents/arch-verifier.md.

Current line 4: `tools: Read, Bash, Grep, Glob`
Change to: `tools: Read, Write, Bash, Grep, Glob`

This adds Write so the verifier can produce VERIFICATION.md directly (execution_flow Step 8 writes this file). Write is inserted after Read to match the tool ordering convention used across other agent specs (Read, Write, ...).

Do NOT modify any other content in the file. The body must remain identical.
  </action>
  <verify>
Run: `head -6 agents/arch-verifier.md` — line 4 should read `tools: Read, Write, Bash, Grep, Glob`.
Run: `grep 'tools:' agents/arch-verifier.md` — should show exactly `tools: Read, Write, Bash, Grep, Glob`.
Run: `wc -l agents/arch-verifier.md` — line count should be 619 (unchanged from original).
  </verify>
  <done>agents/arch-verifier.md frontmatter has `tools: Read, Write, Bash, Grep, Glob`. File body unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: Add Write to arch-researcher tools (PERM-05)</name>
  <files>agents/arch-researcher.md</files>
  <action>
Edit the YAML frontmatter `tools:` line in agents/arch-researcher.md.

Current line 4: `tools: Read, Bash, Grep, Glob, WebFetch`
Change to: `tools: Read, Write, Bash, Grep, Glob, WebFetch`

This adds Write so the researcher can produce RESEARCH.md directly (execution_flow Step 7 already says "Write .arch/RESEARCH.md using the Write tool" but the tool was missing from the frontmatter). Write is inserted after Read to match convention. WebFetch stays at the end as the domain-specific tool.

Do NOT modify any other content in the file. The body must remain identical.
  </action>
  <verify>
Run: `head -6 agents/arch-researcher.md` — line 4 should read `tools: Read, Write, Bash, Grep, Glob, WebFetch`.
Run: `grep 'tools:' agents/arch-researcher.md` — should show exactly `tools: Read, Write, Bash, Grep, Glob, WebFetch`.
Run: `wc -l agents/arch-researcher.md` — line count should be 320 (unchanged from original).
  </verify>
  <done>agents/arch-researcher.md frontmatter has `tools: Read, Write, Bash, Grep, Glob, WebFetch`. File body unchanged.</done>
</task>

</tasks>

<verification>
After all three tasks complete, verify the full phase success criteria:

1. `grep 'tools:' agents/arch-roadmapper.md` outputs `tools: Read, Write, Bash, Grep, Glob` (Write present, Edit absent)
2. `grep 'tools:' agents/arch-verifier.md` outputs `tools: Read, Write, Bash, Grep, Glob` (Write added)
3. `grep 'tools:' agents/arch-researcher.md` outputs `tools: Read, Write, Bash, Grep, Glob, WebFetch` (Write added)
4. `grep -l 'Edit' agents/arch-roadmapper.md agents/arch-verifier.md agents/arch-researcher.md` returns nothing (no agent has Edit that shouldn't)
5. No other agent spec files modified: `git diff --name-only` shows only the 3 files above
6. Body content preserved: `git diff agents/arch-roadmapper.md` shows only line 4 changed; same for the other two files
</verification>

<success_criteria>
- arch-roadmapper tools = Read, Write, Bash, Grep, Glob (Edit removed per PERM-02)
- arch-verifier tools = Read, Write, Bash, Grep, Glob (Write added per PERM-04)
- arch-researcher tools = Read, Write, Bash, Grep, Glob, WebFetch (Write added per PERM-05)
- No other files modified
- All agent spec body content unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/01-permission-boundaries/01-01-SUMMARY.md`
</output>
