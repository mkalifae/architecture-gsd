---
phase: 03-workflow-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/AAA/new-system.md
  - commands/AAA/plan-phase.md
autonomous: true

must_haves:
  truths:
    - "/AAA:new-system command spec shows arch-researcher spawn step producing RESEARCH.md after intake"
    - "/AAA:new-system command spec shows arch-roadmapper spawn step producing ROADMAP.md after research"
    - "/AAA:plan-phase command spec assumes RESEARCH.md and ROADMAP.md exist with no fallback spawning logic"
  artifacts:
    - path: "commands/AAA/new-system.md"
      provides: "Full initialization pipeline: intake -> research -> roadmap"
      contains: "arch-researcher"
    - path: "commands/AAA/new-system.md"
      provides: "Roadmapper spawn step after researcher"
      contains: "arch-roadmapper"
    - path: "commands/AAA/plan-phase.md"
      provides: "Simplified prerequisite check without fallback spawning"
  key_links:
    - from: "commands/AAA/new-system.md"
      to: "agents/arch-researcher.md"
      via: "Task() spawn in new Step 5"
      pattern: "arch-researcher"
    - from: "commands/AAA/new-system.md"
      to: "agents/arch-roadmapper.md"
      via: "Task() spawn in new Step 6"
      pattern: "arch-roadmapper"
    - from: "commands/AAA/plan-phase.md"
      to: "RESEARCH.md and ROADMAP.md"
      via: "existence check that stops if missing"
      pattern: "Run /AAA:new-system"
---

<objective>
Extend `/AAA:new-system` to run the full initialization pipeline (intake -> research -> roadmap) and simplify `/AAA:plan-phase` to assume prerequisites exist.

Purpose: Currently, plan-phase has fallback spawning logic for RESEARCH.md and ROADMAP.md. This means the initialization workflow is split across two commands. By moving researcher and roadmapper spawning into new-system, the pipeline becomes: new-system produces all three foundation files (CONTEXT.md, RESEARCH.md, ROADMAP.md), and plan-phase can focus purely on planning without worrying about prerequisite generation.

Output: Updated new-system.md with 2 new spawn steps, updated plan-phase.md with simplified prerequisite check.
</objective>

<execution_context>
@/home/mkali/.claude/get-shit-done/workflows/execute-plan.md
@/home/mkali/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@commands/AAA/new-system.md
@commands/AAA/plan-phase.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend new-system with arch-researcher and arch-roadmapper spawn steps (WKFL-01, WKFL-02)</name>
  <files>commands/AAA/new-system.md</files>
  <action>
Add two new steps to the new-system.md command spec between the current Step 4 (Handle discuss-system Return) and current Step 5 (Initialize STATE.md). This requires renumbering Steps 5-6 to Steps 7-8.

**New Step 5: Spawn arch-researcher**

Insert after "If status is complete and validation passes, continue to Step 5" (which now becomes "continue to Step 5"):

```
## Step 5: Spawn arch-researcher

Display: "Spawning arch-researcher to analyze system context and produce RESEARCH.md..."

Spawn arch-researcher via Task():
  model: "sonnet"
  prompt: |
    Read agents/arch-researcher.md for your complete execution instructions.
    System context file: .arch/CONTEXT.md
    Produce output file: .arch/RESEARCH.md
    Follow your execution_flow exactly and return structured status JSON.

Wait for completion. Parse return.

If status is "failed": display "arch-researcher failed: [error message]. RESEARCH.md was not produced. You can re-run /AAA:new-system to retry (it will skip intake since CONTEXT.md exists)." and stop. Do not proceed to Step 6.

If status is "complete": continue to Step 6.
```

**New Step 6: Spawn arch-roadmapper**

```
## Step 6: Spawn arch-roadmapper

Display: "Spawning arch-roadmapper to create design roadmap from research findings..."

Spawn arch-roadmapper via Task():
  model: "opus"
  prompt: |
    Read agents/arch-roadmapper.md for your complete execution instructions.
    System context file: .arch/CONTEXT.md
    Research file: .arch/RESEARCH.md
    Produce output file: .arch/ROADMAP.md
    Follow your execution_flow exactly and return structured status JSON.

Wait for completion. Parse return.

If status is "failed": display "arch-roadmapper failed: [error message]. ROADMAP.md was not produced. You can re-run /AAA:new-system to retry (it will skip intake since CONTEXT.md exists)." and stop. Do not proceed to Step 7.

If status is "complete": continue to Step 7.
```

**Renumber existing steps:**
- Current Step 5 (Initialize STATE.md) becomes Step 7
- Current Step 6 (Confirm and Display Next Steps) becomes Step 8

**Update Step 4 references:** In Step 4, where it says "continue to Step 5" for the successful case, this now correctly points to the new Step 5 (arch-researcher). No text change needed since the step number is already 5.

**Update Step 7 (formerly Step 5) — Initialize STATE.md:**
- In the template section, update the "Resume with" line from `/AAA:plan-phase 1` to `/AAA:plan-phase 1` (no change needed — it already points to plan-phase)
- After the STATE.md template, the text "CRITICAL: STATE.md is an index document" stays unchanged.

**Update Step 8 (formerly Step 6) — Confirm and Display Next Steps:**
- Update the git commit command to also add RESEARCH.md and ROADMAP.md:
  `Bash: git add .arch/CONTEXT.md .arch/STATE.md .arch/RESEARCH.md .arch/ROADMAP.md && git commit -m "feat: initialize architecture project"`
- Update the confirmation display to include the new files:
  ```
  AAA -- Project Initialized

  System: [domain from CONTEXT.md]
  Context:  .arch/CONTEXT.md (validated)
  Research: .arch/RESEARCH.md (produced)
  Roadmap:  .arch/ROADMAP.md (produced)
  State:    .arch/STATE.md (initialized)

  Locked decisions: [count] decisions locked at intake
  Non-goals: [count] explicit non-goals

  Next: /AAA:plan-phase 1 (after /clear for fresh context)
  ```

**Also add skip logic for Steps 5-6:** At the top of Step 5, add:
```
Check if RESEARCH.md already exists:
  Bash: ls .arch/RESEARCH.md 2>/dev/null && echo "exists" || echo "missing"

If exists: Display "RESEARCH.md already exists — skipping arch-researcher." and continue to Step 6.
```

At the top of Step 6, add the same pattern for ROADMAP.md:
```
Check if ROADMAP.md already exists:
  Bash: ls .arch/ROADMAP.md 2>/dev/null && echo "exists" || echo "missing"

If exists: Display "ROADMAP.md already exists — skipping arch-roadmapper." and continue to Step 7.
```

This ensures re-running /AAA:new-system after a partial failure skips already-completed steps.

**Do NOT modify:** The purpose section, Steps 1-3, or the allowed-tools frontmatter. The existing Task tool in allowed-tools already covers spawning subagents.
  </action>
  <verify>
Read the modified commands/AAA/new-system.md and confirm:
1. Steps are numbered 1 through 8 (was 1 through 6)
2. Step 5 spawns arch-researcher with model "sonnet" and produces .arch/RESEARCH.md
3. Step 6 spawns arch-roadmapper with model "opus" and produces .arch/ROADMAP.md
4. Both new steps have skip-if-exists logic
5. Both new steps have failure handling that stops execution
6. Step 8 git commit includes RESEARCH.md and ROADMAP.md
7. Step 8 confirmation display shows Research and Roadmap lines
  </verify>
  <done>new-system.md contains 8 steps with arch-researcher spawn at Step 5 and arch-roadmapper spawn at Step 6, both with skip-if-exists and failure handling. WKFL-01 and WKFL-02 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: Simplify plan-phase prerequisite check to assume RESEARCH.md and ROADMAP.md exist (WKFL-03)</name>
  <files>commands/AAA/plan-phase.md</files>
  <action>
Replace the current Step 2 (Check Prerequisites) in plan-phase.md. The current Step 2 spawns arch-researcher if RESEARCH.md is missing and spawns arch-roadmapper if ROADMAP.md is missing. Replace this with a simple existence check that tells the user to run /AAA:new-system if either file is missing.

**Replace the entire current Step 2 content with:**

```
## Step 2: Check Prerequisites

Verify that the initialization pipeline has been run:

  Bash: ls .arch/RESEARCH.md 2>/dev/null && echo "exists" || echo "missing"

If missing:
  Display: "No RESEARCH.md found. Run /AAA:new-system first — it produces CONTEXT.md, RESEARCH.md, and ROADMAP.md as part of the initialization pipeline."
  Stop.

  Bash: ls .arch/ROADMAP.md 2>/dev/null && echo "exists" || echo "missing"

If missing:
  Display: "No ROADMAP.md found. Run /AAA:new-system first — it produces CONTEXT.md, RESEARCH.md, and ROADMAP.md as part of the initialization pipeline."
  Stop.

Both prerequisites confirmed. Continue to Step 3.
```

**Key changes from the current version:**
- REMOVE the "Spawn arch-researcher via Task()" block and its model/prompt configuration
- REMOVE the "Spawn arch-roadmapper via Task()" block and its model/prompt configuration
- REMOVE the "Wait for completion. Parse return. If status is failed" error handling for both spawns
- REMOVE the line "If both RESEARCH.md and ROADMAP.md already exist, skip this step entirely"
- REPLACE all of the above with simple existence checks that redirect to /AAA:new-system
- The error messages must mention /AAA:new-system as the fix, not suggest running researcher/roadmapper directly

**Update the purpose section** to reflect the simplified role. Change:
```
coordinate the planning pipeline: run arch-researcher (if no RESEARCH.md exists), run arch-roadmapper (if no ROADMAP.md exists), enter the arch-planner + arch-checker bounded revision loop
```
to:
```
coordinate the planning pipeline: verify prerequisites exist (RESEARCH.md, ROADMAP.md from /AAA:new-system), enter the arch-planner + arch-checker bounded revision loop
```

**Update success_criteria section:** Change the line:
```
- [ ] RESEARCH.md produced (or already existed)
- [ ] ROADMAP.md produced (or already existed)
```
to:
```
- [ ] RESEARCH.md verified to exist (produced by /AAA:new-system)
- [ ] ROADMAP.md verified to exist (produced by /AAA:new-system)
```

**Do NOT modify:** Steps 1, 3-8, the allowed-tools frontmatter, or the arch-planner/arch-checker spawn logic.
  </action>
  <verify>
Read the modified commands/AAA/plan-phase.md and confirm:
1. Step 2 contains NO "Spawn arch-researcher" or "Spawn arch-roadmapper" blocks
2. Step 2 contains NO "Task()" calls
3. Step 2 contains existence checks for RESEARCH.md and ROADMAP.md
4. Step 2 error messages reference "/AAA:new-system" as the fix
5. Purpose section no longer mentions "run arch-researcher" or "run arch-roadmapper"
6. Success criteria reference "/AAA:new-system" for prerequisite production
7. Steps 3-8 are unchanged
  </verify>
  <done>plan-phase.md Step 2 is a simple existence check with no fallback spawning logic. Error messages direct users to /AAA:new-system. WKFL-03 satisfied.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase:

1. Read commands/AAA/new-system.md — confirm it has 8 numbered steps, with Steps 5-6 spawning arch-researcher and arch-roadmapper respectively
2. Read commands/AAA/plan-phase.md — confirm Step 2 has NO Task() spawn calls, only existence checks
3. Grep for "Spawn arch-researcher" in new-system.md — should find it
4. Grep for "Spawn arch-researcher" in plan-phase.md — should NOT find it
5. Grep for "Spawn arch-roadmapper" in new-system.md — should find it
6. Grep for "Spawn arch-roadmapper" in plan-phase.md — should NOT find it
</verification>

<success_criteria>
- new-system.md contains arch-researcher spawn step (WKFL-01)
- new-system.md contains arch-roadmapper spawn step (WKFL-02)
- plan-phase.md has no fallback spawning logic for RESEARCH.md or ROADMAP.md (WKFL-03)
- Both files maintain consistent step numbering
- Error messages guide users to the correct command
</success_criteria>

<output>
After completion, create `.planning/phases/03-workflow-restructure/03-01-SUMMARY.md`
</output>
